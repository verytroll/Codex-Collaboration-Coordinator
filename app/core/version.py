"""Canonical application and release version metadata."""

from __future__ import annotations

APP_VERSION = "0.3.0"
RELEASE_TAG = f"v{APP_VERSION}"
RELEASE_CANDIDATE = f"{RELEASE_TAG}-rc.1"
RELEASE_BASELINE_NAME = f"codex-collaboration-coordinator-{APP_VERSION}"
